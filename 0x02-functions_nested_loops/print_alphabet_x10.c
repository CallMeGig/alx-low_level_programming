#include "main.h"

/**
 * _islower - checks for lowecase
 *
 * Returns: 1 in c is lowercase, 0 onterwiae
 */

int _islower(int i)
{
	int j
	if('a' <= c && c <= 'z')
		j = 1;
	else
		j = 0
	return (j);
}
